/*These Time Intervals are called Timing Events

There are two key methods to use with JavaScript :

setTimeout(function,milliseconds):It executes a function after waiting for given
number of milliseconds.


setInterval(function,milliseconds):It repeats the execution of Function after 
the number of milliseconds

*/
